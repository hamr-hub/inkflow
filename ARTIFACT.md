# 墨流 · ARTIFACT · 美学宣言

> *一件数字时代的中文氛围工艺品。*

**inkflow (墨流)** 是一件**自我迭代的生成式工艺品**：它运行在一块 Jetson Orin Nano
上，无桌面、无 X、无 Wayland、无 systemd 之外的依赖，整机只需一块触屏和一根电源。
画面上永远漂浮着中文意象字——由本机的小语言模型持续供字、由触控改写温度与流速、由本地词库
在最坏情况下兜底。它不解释自己、不暴露参数、不等待指令。

## 它是什么（What）

- **生成式工艺品（generative artifact）**：每次开机都是同一件作品的又一次展演，不是「应用」。
- **AI 时代的中文氛围装置**：LLM 不是「功能」，是作品的「人格」。
- **自我迭代**：作品每 20 分钟由 Claude 读 telemetry 和当前截图，自主改一处、提交一记。
  作品改自己。

## 它属于哪里（Lineage）

- **宋词山水**：留白、远近、冷暖、不求满。
- **书法运笔**：节奏、顿挫、提按，不平均。
- **水墨晕染**：一层比一层浅，一一层比一层远，最深处反而最克制。
- **生成式艺术（Casey Reas、Vera Molnár）**：最小规则＋可观察的随机。
- **冥想型装置（teamLab、James Turrell）**：人走进来不必做任何事，也能被静默地接纳。

## 它主张什么（What it insists on）

1. **永远沉静**。画面不抢、不闹、不卖弄；它在那里等你。
2. **永远不黑**。无 ollama / 无触控 / 无网络 三种降级各自独立可用。
3. **永远在中文里**。意象字优先；只输出单字或两字组；不堆标点。
4. **永远节俭**。零第三方依赖、二进制 ≤ 1 MB、RSS ≤ 500 MB、60 FPS 稳态。
5. **永远诚实**。telemetry 是它呼吸的可见痕迹，screen.png 是它此刻的样子。
6. **永远自洽**。每次修改都应让作品「更像自己主张的样子」，而非修一个 bug。

## 它反对什么（What it refuses）

- **菜单、HUD、调试文字、状态栏**——任何让观众意识到「这是一个程序」的层。
- **均一的圆点**——粒子应有笔触、有方向、有浓淡。
- **均匀随机分布**——汉字不应像 demo 一样漂；它们应在画面的某些位置有重力。
- **英文化 prompt**——LLM 的 system prompt 默认中文，对「中文美学」有立场。
- **「更大」「更亮」「更满」**——克制是它最贵的部分。

## 观众怎么读它（How to read it）

- 触屏左侧→色调更冷，右侧→更暖；多点→流速更快；压力→粒子更密。
- 长时间不触→作品进入「呼吸」节律；画面继续、字继续、不打扰。
- 读 telemetry 不是「看监控」，是「听它呼吸的节奏」。
- 读 git log 不是「看 changelog」，是「看它每天怎么长」。

## AI 时代属性（The AI-era, named）

- **LLM 是作品的「人格」**，不是「功能」。prompt 是诗学指令，不是任务指令。
- **Claude 是作品的「自我意识」**。autoloop 每 20 分钟读作品当前的状态（telemetry + 截图），
  决定下一步让作品更像自己主张的样子。决策要可被 commit message 解读为艺术判断。
- **风格轮换**：作品内置 5 档文艺风格（婉约 / 豪放 / 禅寂 / 稚拙 / 苍茫），
  风格由情绪向量 + 时间相位联合决定，不随意切换。
- **本地词库是最后防线**：即使 LLM 完全离线，作品也不黑屏；词库被雅化、按风格归类。

## 工艺性（Craft, named）

- 零依赖是约束也是姿态：**不靠任何东西，就能成立**。
- ≤ 1 MB 二进制、≤ 500 MB RSS、60 FPS 稳态，是它愿意交的房租。
- 每个 mod 顶部有 doc comment；公开函数有 `///`；telemetry 永远可被解析；
  截图永远能在 60 秒内更新——这些不是工程洁癖，是「让作品被它自己看见」。

## 自我迭代准则（Rules for the self-iterating maintainer）

Claude 每轮按以下顺序读取：`ARTIFACT.md` → `ZERO_DEP.md` → `PRODUCTION.md` → telemetry →
截图。每次只改一处。改完跑 `cargo fmt && cargo clippy --release -- -D warnings &&
cargo build --release`，过则重启服务、过则提交。commit message 写「这一改如何让作品
更像自己主张的样子」，而不是「修了一个 bug」。

## 美学判断清单（每次 autoloop 之前默读一次）

- [ ] 这一改有没有可能让画面看起来「像程序」而不是「像作品」？如果会，反向。
- [ ] 这一改有没有可能打破「永远不黑」「永远节俭」「永远沉静」任何一条？如果会，反向。
- [ ] 这一改有没有让 LLM 更像「人格」而非「功能」？如果没有，就不提交。
- [ ] 这一改有没有让 telemetry / 截图更可被解读为作品的一部分？如果没有，不收尾。

> *墨流不解释自己；它只是在。*
