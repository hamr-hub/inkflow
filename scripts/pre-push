#!/usr/bin/env bash
# inkflow pre-push gate (local auto-commits still get fmt+build via autoloop)
set -euo pipefail
export CARGO_TARGET_DIR=/mnt/ssd/codespace/.cargo-target/inkflow
cargo fmt --check
cargo clippy --release -- -D warnings
cargo build --release
