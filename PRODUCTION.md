# inkflow 生产验收标准（Production Readiness）

工艺品本体必须 24h×7 无人值守运行。所有指标用真实证据验证，不接受口头结论。

## 性能（Jetson Orin Nano 8GB）
- [ ] app RSS ≤ 500MB（稳态），峰值 ≤ 800MB；绝不触发整机 OOM
- [ ] 稳态帧率 ≥ 60 FPS（真实 present 帧率，不是空转计数）；帧时间稳定无尖刺
- [ ] 满载时 CPU 占用 ≤ 2 核；GPU/GL 呈现正常，无过热降频
- [ ] glyph/particle 数量有硬上限，分配走复用池而非每帧 Vec 增删
- [ ] 帧截图（PPM 编码）不阻塞渲染线程，单帧开销 < 2ms

## LLM 供字
- [ ] ollama warm 后真实 tok/s 记录在 telemetry；低负载下 ≥ 10 tok/s
- [ ] ollama 冷启动/超时/断连任何情况下，画面 1s 内无缝切本地词库
- [ ] 模型加载失败有退避，不疯狂重连打爆 CPU
- [ ] 支持运行时切换模型（配置文件，无需改代码）

## 可靠性
- [ ] 连续运行 24h：无内存泄漏、无崩溃、无卡死、帧率不衰减
- [ ] `kill -9` 后 systemd 自动拉起 ≤ 5s，画面恢复
- [ ] 触控屏热插拔：开机前/中插入都能识别，拔出回自主呼吸
- [ ] 无触控屏、无网络、ollama 宕机三种降级状态各自独立可用
- [ ] 日志轮转正常，磁盘占用有上限，7 天不撑爆
- [ ] systemd 单元：内存上限、自动重启、开机自启全部验证

## 呈现
- [ ] 真全屏（无窗口边框），分辨率自适应实际屏幕
- [ ] 断电重启后自动回到摆件画面，无需登录/鼠标
- [ ] 中文字体随包可用或路径有 fallback，缺字体不崩

## 验证方式
- 真实长时间运行 + telemetry 数据 + 截图 + journalctl 证据
- 每项改动只在性能/可靠性上推进；不牺牲沉静氛围
- 不显著的改动如实记录并回退
